<div class="ui raised segment">
    <h1>You have {{friendRequests.length}} requests</h1>
</div>

<div class="section">
    <div class="ui link cards">
    {{#each friendRequests}}
        {{> friend_card}}
    {{/each}}
    </div>
</div>