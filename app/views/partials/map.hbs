<style>
    #map {
        width: 100%;
        height: 460px;
        background-color: grey;
    }
</style>
<h3>Start Location of Walkway : {{trail.trailname}}</h3>
<!--The div element for the map -->
<div id="map"></div>

<script>
    // Initialize and add the map
    function initMap() {
        // The location of Uluru
        var trail = {lat: {{trail.startcoordinates.latitude}}, lng: {{trail.startcoordinates.longitude}} };
        // The map, centered at Uluru
        var map = new google.maps.Map(
                document.getElementById('map'), {zoom: 17, center: trail, mapTypeId: 'hybrid'});
        // The marker, positioned at Uluru
        var marker = new google.maps.Marker({position: trail, map: map});
    }
</script>
<!--Load the API from the specified URL
* The async attribute allows the browser to render the page while the API loads
* The key parameter will contain your own API key (which is not needed for this tutorial)
* The callback parameter executes the initMap() function
-->
<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAmqFv56wJAJ8_HWK553x58KWqyE8MmdUw&callback=initMap">
</script>